import React from "react";
import "./StartButton.scss";

const StartButton = () => {
  return (
    <div className='startButton'>
      <button>시작하기</button>
    </div>
  );
};

export default StartButton;
